---
inclusion: auto
---

# NPM Command Formatting

When mentioning npm commands in conversation (e.g., `npm run dev`, `npm install`, `npm test`), always format them as bash code blocks:

```bash
npm run dev
```

This makes them clickable for the user. Never write npm commands as inline text in a sentence.
